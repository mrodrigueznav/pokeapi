DATABASE_URL="postgresql://user:password@localhost:5432/tcg_deck_inventory"
# Required for migrations when using a connection pooler (e.g. Supabase pooler on :6543).
# Use the direct Postgres URL (e.g. Supabase :5432). For local dev, can match DATABASE_URL.
DIRECT_DATABASE_URL="postgresql://user:password@localhost:5432/tcg_deck_inventory"
PORT=4000
POKEMON_TCG_API_KEY=""
