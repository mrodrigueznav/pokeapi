DATABASE_URL="postgresql://postgres.[project-ref]:[PASSWORD]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true"
# Migrations ONLY — Session/direct connection on port 5432 (NOT 6543, NO pgbouncer=true)
# Copy from Supabase → Project Settings → Database → Connection string → Session pooler (5432)
DIRECT_DATABASE_URL="postgresql://postgres.[project-ref]:[PASSWORD]@aws-0-[region].pooler.supabase.com:5432/postgres"
PORT=4000
POKEMON_TCG_API_KEY=""

# If your password contains special characters, URL-encode them in BOTH URLs:
#   @ → %40    # → %23    % → %25
